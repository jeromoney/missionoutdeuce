# Page Logic
 - When an incident is first created, two events are created. Incident created and Incident paged. When firest created, the incident should be paged to dispatchers only.
 - Subsequent updates are either paged to everyone (responders), just dispatchers or not all. This behavior will be controlled by an option in the UI that defaults to everyone
