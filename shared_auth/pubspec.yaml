name: shared_auth
description: Shared auth model for MissionOut Flutter apps
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.3.0
  http: ^1.5.0

dev_dependencies:
  flutter_lints: ^6.0.0
