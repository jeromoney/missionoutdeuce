# Shared Theme

This package is the home for shared design tokens and theme primitives used across MissionOut Flutter apps.

Good candidates:

- common brand colors
- semantic colors such as success, warning, and danger
- border radii
- spacing tokens
- typography tokens

Keep app-specific layouts and widgets inside each app unless they are truly identical.
