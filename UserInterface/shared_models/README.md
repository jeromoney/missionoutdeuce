# Shared Models

This package is the home for MissionOut data contracts that should be shared across client apps.

Good candidates:

- roles
- response status enums
- delivery status enums
- incident state enums
- common API payload models that are stable across apps

Avoid putting app-specific UI models here unless they are truly shared.
