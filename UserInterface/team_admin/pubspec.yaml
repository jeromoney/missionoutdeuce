name: missionout_team_admin
description: MissionOut team administration web app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_auth:
    path: ../shared_auth
  shared_models:
    path: ../shared_models
  shared_theme:
    path: ../shared_theme

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
